import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginTop: 20,
  },
  card: {
    marginTop: 20,
    minHeight: 100,
    minWidth: 100,
  },
  cardImage: {
    resizeMode: "stretch",
  },
  cardText: {
    textAlign: "center",
    paddingTop: 20,
  },
});

export { styles };
